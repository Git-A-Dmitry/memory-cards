// const currentEl = document.getElementById('current');
// let currentActiveCard = 0;

// function updateCurrentText() {
//   let currentActiveCard = 0;
//   const currentEl = document.getElementById('current');

//   currentEl.innerText = `${currentActiveCard + 1} / ${cardsEl.length}`;
// }

// export default updateCurrentText;
